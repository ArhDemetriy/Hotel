'use strict'; {
  function handler(event) {

  }

  const elements = document.querySelectorAll(`.dropfield`);
  elements.forEach((_, i, a) => {
    // click change
    // a[i].addEventListener('change', handler, { passive: true, });
  });
}